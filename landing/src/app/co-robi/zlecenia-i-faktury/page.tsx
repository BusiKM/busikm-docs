import { PageShell, pageMetadata } from '@/components/layout/PageShell';

export const metadata = pageMetadata('co-robi/zlecenia-i-faktury');

export default function Page() {
  return <PageShell slug="co-robi/zlecenia-i-faktury" />;
}
