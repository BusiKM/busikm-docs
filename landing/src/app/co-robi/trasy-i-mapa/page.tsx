import { PageShell, pageMetadata } from '@/components/layout/PageShell';

export const metadata = pageMetadata('co-robi/trasy-i-mapa');

export default function Page() {
  return <PageShell slug="co-robi/trasy-i-mapa" />;
}
