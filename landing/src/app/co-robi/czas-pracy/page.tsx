import { PageShell, pageMetadata } from '@/components/layout/PageShell';

export const metadata = pageMetadata('co-robi/czas-pracy');

export default function Page() {
  return <PageShell slug="co-robi/czas-pracy" />;
}
