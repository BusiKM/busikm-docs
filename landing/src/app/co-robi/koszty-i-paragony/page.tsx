import { PageShell, pageMetadata } from '@/components/layout/PageShell';

export const metadata = pageMetadata('co-robi/koszty-i-paragony');

export default function Page() {
  return <PageShell slug="co-robi/koszty-i-paragony" />;
}
