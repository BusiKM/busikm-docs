import { PageShell, pageMetadata } from '@/components/layout/PageShell';

export const metadata = pageMetadata('dla-kogo/ksiegowa');

export default function Page() {
  return <PageShell slug="dla-kogo/ksiegowa" />;
}
