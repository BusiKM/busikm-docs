import { PageShell, pageMetadata } from '@/components/layout/PageShell';

export const metadata = pageMetadata('dla-kogo');

export default function Page() {
  return <PageShell slug="dla-kogo" />;
}
