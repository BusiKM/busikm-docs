import { PageShell, pageMetadata } from '@/components/layout/PageShell';

export const metadata = pageMetadata('dla-kogo/dyspozytor');

export default function Page() {
  return <PageShell slug="dla-kogo/dyspozytor" />;
}
